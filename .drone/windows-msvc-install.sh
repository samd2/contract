#!/bin/bash

# This script is not presently used but Microsoft Windows configuration could go here in the future.

set -ex

# move to Dockerfile:
# if  [ "$CXX" = "msvc-14.2" -o "$B2_TOOLSET" = "msvc-14.2" ]; then
#   choco install visualstudio2019-workload-vctools --package-parameters "--add Microsoft.VisualStudio.Component.VC.14.24.x86.x64" -y
# fi

true

